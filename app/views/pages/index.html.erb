<!DOCTYPE html>
<html lang="en">
<head>
  <div class="navbar navbar-inverse navbar-fixed-top" style="background-color:transparent; height:94px; padding:none; border-color:transparent;">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" style="margin-top:25px;"aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

    <a class="navbar-brand" style=""><%=image_tag "elem_logo-white.png", :style => "width: 95%;"%> </a>
    <div class="collapse navbar-collapse" style="background:transparent;" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right" style=" padding:none;">
        <% if user_signed_in? %>`
        <li role="presentation" class="dropdown" style=" margin:20px 60px 0px 20px;">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false" style="font-size:19px;margin:none; color:white;">
            <%= current_user %> <span class="caret"></span>
          </a>
          
          <ul class="dropdown-menu">
            <li>
                <p class="" href="#login" data-toggle="modal"><center><%= link_to "PROFILE", hound_user_path(current_user.id), :style=>"font-size:14px; margin-left:12px;" %></center></p>
            </li>
            <li><hr></li>
            <li>
              <% if session[:fb_token].nil? %>
                <p class="" href="#login" data-toggle="modal"><center><%= link_to "SIGN OUT", destroy_user_session_path, :style=>"font-size:14px; margin-left:12px;" %></center></p>
              <% else %>
                <p class="" href="#login" data-toggle="modal"><center><%= link_to "SIGN OUT", facebook_logout_path, :style=>"font-size:14px; margin-left:12px;" %></center></p>
              <% end %>
            </li>
          </ul>
        </li>
        <%else%>
        <li style=" margin:20px 10px 0px 20px;"><a style="font-size:22px;margin:none; color:white;" href="#login" data-toggle="modal"><b>LOG IN</b></a></li>

         <li style=" margin:20px 50px 0px 10px;"><a style="font-size:22px;margin:none; color:white;" href="#signup" data-toggle="modal"><button style="height: 40px; width:150px; border-radius: 20px; border: solid 1px; background-color: transparent; margin-top:-10px;"><b>SIGN UP</b></button></a></li>
        <% end %>
      </ul>
    </div>          
  </div>
</head>

<body id="landing">
<section id="index_jum" class="mouse-bg row" data-autoheight="true">
</section><!--end hero-->

<%= render 'modal' %>

 <div class="landing-search">
 <center>
 <span class="lantitle"><b>What's the plan?</b></span><br>
  <form action="/search">
   <input id="sb-1" name="search_query" class="lanSearch" placeholder="" size="14" autofocus="autofocus"><br>
    <input type="submit" value="Submit" class="hide"/>
  </form>
  </center>
</div>
</body>

<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
<script src="assets/mouse.parallax.js"></script>

<script>
$(document).ready(function() {
$('#index_jum').mouseParallax({ moveFactor: 1 });
$('body').height(1000);
});
/* FOR FOCUS
$( document ).keypress(function() {
  $("#sb-1").focus();
});
*/

</script>

<script type="text/javascript">
function shuffle(array) {
  var currentIndex = array.length, temporaryValue, randomIndex ;
  // While there remain elements to shuffle...
  while (0 !== currentIndex) {

    // Pick a remaining element...
    randomIndex = Math.floor(Math.random() * currentIndex);
    currentIndex -= 1;

    // And swap it with the current element.
    temporaryValue = array[currentIndex];
    array[currentIndex] = array[randomIndex];
    array[randomIndex] = temporaryValue;
  }

  return array;
}
  var images = ["elem_splash-img.jpg", "download1.jpeg","download3.jpeg"];
  shuffle(images);
  console.log(images);
  var i = 0;
      $("#index_jum").css("background-image", "url(/assets/" + images[i] + ")");
  /* FOR FADE EFFECT
  $(function () {
      var i = 0;
      $("#index_jum").css("background-image", "url(/assets/" + images[i] + ")");
      setInterval(function () {
          i++;
          if (i == images.length) {
              i = 0;
          }
          $("#index_jum").fadeOut(500, 'swing', function () {
              $(this).css("background-image", "url(/assets/" + images[i] + ")");
              $(this).fadeIn(500);
          });
      }, 25000);
  });*/
</script>
</html>

